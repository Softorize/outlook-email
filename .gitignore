# Zig build artefacts
zig-out/
.zig-cache/
zig-cache/

# Editor / OS noise
.DS_Store
*.swp
*.swo
*~
.idea/
.vscode/

# Local credential / config files (just in case anyone runs the binary
# from inside the repo)
credentials.enc
seed
config.ini
